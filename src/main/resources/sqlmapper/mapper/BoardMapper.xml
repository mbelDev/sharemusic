<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.sharemusic.dao.BoardDao">
 <select id="getPostAll" resultType="BoardDto">
    select * from BOARD ORDER BY POSTNO 
  </select>
  <select id="getTotalPost" resultType="Integer">
	  SELECT COUNT(*) AS TOTAL FROM BOARD
  </select>
  <insert id="putPost" parameterType="BoardDto">
	  INSERT INTO BOARD VALUES(BOARD_SEQ.NEXTVAL,#{postAuth},#{postPW},#{postCTGR},#{postTitle},#{postCont},SYSDATE,0)
  </insert>
  <delete id="deletePost" parameterType="Integer">
	  DELETE FROM BOARD WHERE POSTNO = #{postNo}
  </delete>
  <update id="updateHits" parameterType="Integer">
	  UPDATE BOARD SET POSTHITS=POSTHITS+1 WHERE POSTNO = #{postNo}
  </update>
  <update id="updatePost" parameterType="BoardDto">
	  UPDATE BOARD SET postCTGR = #{postCTGR}, postTitle = #{postTitle}, postCont = #{postCont} WHERE postNO = #{postNo} 
  </update>

</mapper>